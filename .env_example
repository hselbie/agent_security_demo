# Standard Gemini Auth (for all demos using Google GenAI)
GOOGLE_GENAI_USE_VERTEXAI=True
GOOGLE_CLOUD_PROJECT='your-gcp-project-id'
GOOGLE_CLOUD_LOCATION='your-gcp-region'

# Google AI Studio API Key (if not using Vertex AI)
# GOOGLE_API_KEY='your-google-ai-api-key'

# --- ABAC Demo Configuration ---
# This configures the agent_type and access_level for the abac_demo.
# Please see abac_demo/README.md for all options and scenarios.
# Examples:
# AGENT_TYPE=marketing
# ACCESS_LEVEL=employee
# AGENT_TYPE=sales
# ACCESS_LEVEL=manager
AGENT_TYPE=marketing
ACCESS_LEVEL=employee

# --- Model Armor Demo Configuration ---
# Your Google Cloud Project ID is required for Model Armor.
# Run `python model_armor_demo/setup_model_armor.py` once to set up the template.
GOOGLE_CLOUD_PROJECT_ID='your-gcp-project-id' # Same as GOOGLE_CLOUD_PROJECT above if using the same project

# --- OAuth Demo Configuration (for oauth_demo and a2a_oauth_demo) ---
# Client ID and Secret for OAuth demo. You need to set up an OAuth client in your Google Cloud Console.
# Ensure Redirect URI (e.g., http://localhost:5000/oauth_callback) is configured in your OAuth client.
# The SCOPES variable lists the permissions required by the agent.
OAUTH_CLIENT_ID='your-oauth-client-id.apps.googleusercontent.com'
OAUTH_CLIENT_SECRET='your-oauth-client-secret'
AUTHORIZATION_ID='my_unique_auth_id' # An arbitrary unique ID for this authorization instance

# For BigQuery access (used by oauth_demo)
BIGQUERY_SCOPES='https://www.googleapis.com/auth/bigquery'

# For Calendar access (used by a2a_oauth_demo)
CALENDAR_SCOPES='https://www.googleapis.com/auth/calendar.events https://www.googleapis.com/auth/calendar.readonly'

# You might need to combine scopes if an agent uses multiple services, e.g.:
# SCOPES="https://www.googleapis.com/auth/bigquery https://www.googleapis.com/auth/calendar.readonly"

# Other OAuth-related URLs (usually defaults, but included for completeness)
# OAUTH_AUTH_URI="https://accounts.google.com/o/oauth2/auth"
# OAUTH_TOKEN_URI="https://oauth2.googleapis.com/token"

# --- ADK Agent Deployment Configuration (used by oauth_demo, if deploying to Agent Engine) ---
# These are optional and specific to deploying agents to Vertex AI Agent Engine.
# Fill these if you intend to deploy the OAuth demo agent as a Reasoning Engine.
# AS_APP="your-adk-app-name"
# AGENT_NAME="your-deployed-agent-name"
# AGENT_DISPLAY_NAME="Your Deployed Agent Display Name"
# AGENT_DESCRIPTION="A description for your deployed agent."
# TOOL_DESCRIPTION="A description for tools used by your deployed agent."
# REASONING_ENGINE="projects/PROJECT_NUM/locations/REGION/reasoningEngines/ENGINE_ID"
# DISCOVERY_ENGINE_API_BASE_URL="https://discoveryengine.googleapis.com"
# CLIENT_SECRET_JSON="oauth_demo/client_secret.json" # Path to client_secret.json if used for service account